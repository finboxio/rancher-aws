#! /bin/bash
export NODE_IP=$(curl -s http://instance-data/latest/dynamic/instance-identity/document | jq -r .privateIp)
if [[ ! $NODE_IP ]]; then
    echo "$pkg: failed to get ip"
    exit 1
fi

echo $NODE_IP
